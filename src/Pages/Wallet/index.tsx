import React from 'react';
import { AppContent } from '@Src/Components/AppContent';
import { Title } from './styles';


export const Wallet = () => {
    return (
        <AppContent>
            <Title>Wallet</Title>
        </AppContent>
    )
}